<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#1E1B26"
    android:padding="24dp"
    android:gravity="center_vertical">

    <!-- Heading -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create an account"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="8dp"/>

    <!-- Subtext -->
    <TextView
        android:id="@+id/tvLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Already have an account? Log in"
        android:textSize="14sp"
        android:textColor="#AAAAAA"
        android:layout_marginBottom="32dp"/>

    <!-- Signup as Voter Button -->
    <Button
        android:id="@+id/btnSignupVoter"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="Sign up as Voter"
        android:textAllCaps="false"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:background="@drawable/bg_button_purple"
        android:layout_marginBottom="16dp"/>


    <!-- Signup as Candidate Button -->
    <Button
        android:id="@+id/btnSignupCandidate"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="Sign up as Candidate"
        android:textAllCaps="false"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:background="@drawable/bg_button_purple"
        android:layout_marginBottom="16dp"/>

    <!-- Signup as Admin Button -->
    <Button
        android:id="@+id/btnSignupAdmin"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="Sign up as Admin"
        android:textAllCaps="false"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:background="@drawable/bg_button_purple"
        android:layout_marginBottom="24dp"/>

    <!-- Create Account Button -->
    <Button
        android:id="@+id/btnCreate"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="Create account"
        android:textAllCaps="false"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:background="@drawable/bg_button_purple"/>
</LinearLayout>